using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class RandomSpawnItem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int Randoms = Random.Range(1, 4);
        Instantiate(spawnedObject[Randoms], transform.position, transform.rotation);
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void SpawnCoins()
    {
        int Coins = 0;
        for (int i = 0; i < 5; ++i)
        {
            GameObject Coins = Instantiate(gameobject);
            Coins.transform.position = new Vector3(position.x, position.y + transform.localScale.y / 2, position.z);
        }
    }
}
*/
